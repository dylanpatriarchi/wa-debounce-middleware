# WhatsApp Configuration
# Token for webhook verification (you set this in Meta Dashboard)
WHATSAPP_VERIFY_TOKEN=my-secret-verify-token
# App Secret for HMAC signature validation (Meta App Dashboard > Basic Settings)
WHATSAPP_APP_SECRET=

# n8n Configuration
# The URL of your n8n Webhook node
N8N_WEBHOOK_URL=https://n8n.example.com/webhook/...

# Upstash Redis Configuration (Required for Debounce)
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Logic Configuration
# Time to wait (in ms) before sending to n8n. Default: 2000
DEBOUNCE_WINDOW_MS=2000

# Security / Filtering (Optional)
# Comma-separated list of allowed phone numbers (e.g. 15550000000)
# ALLOWED_NUMBERS=
# Comma-separated list of blocked phone numbers
# BLOCKED_NUMBERS=